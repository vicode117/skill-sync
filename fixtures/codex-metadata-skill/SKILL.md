---
name: codex-metadata-skill
description: A skill carrying tool-specific metadata alongside common content.
metadata:
  openai:
    fallback_model: gpt-4.1
---

# Codex Metadata Skill

The `agents/openai.yaml` and `metadata.openai` entries are tool-specific
extensions that must survive synchronization untouched.
